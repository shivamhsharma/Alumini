<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pratik.alumni_database">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".dataset" />
        <activity android:name=".register" />
        <activity android:name=".personaldetails" />
        <activity android:name=".AcademicDetails" />
        <activity android:name=".SSCDetails" />
        <activity android:name=".HSC_Detais" />
        <activity android:name=".Engineering_Details"></activity>
    </application>

</manifest>